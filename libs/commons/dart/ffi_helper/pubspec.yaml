name: ffi_helper_ab
description: "Dart Foreign Function Interface (FFI) helper classes and functions to aid with native code integration."
version: 0.0.1
homepage: https://github.com/appbricks/mycloudspace/tree/main/libs/commons/dart/ffi_helper

environment:
  sdk: ">=3.2.6 <4.0.0"

dependencies:
  ffi: ^2.1.0
  async_helper: ^0.0.1

dev_dependencies:
  lints: ^3.0.0
  test: ^1.25.2

platforms:
  android:
  ios:
  linux:
  macos:
  web:
  windows:

flutter:
